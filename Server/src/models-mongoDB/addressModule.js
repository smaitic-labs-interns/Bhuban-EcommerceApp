const Address = ({country, province, city, ward, tole, houseNo}) => {
    return { 
        country,
        province, 
        city, ward,
        tole, 
        houseNo 
    };
}

module.exports = {Address}
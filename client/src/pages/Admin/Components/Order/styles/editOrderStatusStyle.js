import { Box, styled, Typography } from '@mui/material';

export const OrderStatusWrapper = styled(Box)(() => ({
  display: 'flex',
  justifyContent: 'space-between',
  alignItems: 'center',
  border: 'solid 2px grey',
  borderRight: 'none',
  borderLeft: 'none',
  padding: '1rem',
}));

export const LeftWrapper = styled(Box)(() => ({}));

export const LeftTitleWrapper = styled(Typography)(() => ({}));

export const RightWrapper = styled(Box)(() => ({}));
export const MiddleWrapper = styled(Box)(() => ({}));

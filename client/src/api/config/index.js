import { axios_instance } from "api/config/baseApi";
import {
  user,
  product,
  cart,
  mail,
  address,
  order,
} from "api/config/api-endpoints";

export { axios_instance, user, product, cart, mail, address, order };
